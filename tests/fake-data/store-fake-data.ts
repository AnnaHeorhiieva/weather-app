import { currentStateFakeData } from "./current-state-fake-data";
import { hourlyStateFakeData } from "./hourly-state-fake-data";

export const storeFakeData = {
  currentWeather: currentStateFakeData,
  hourlyWeather: hourlyStateFakeData,
};
